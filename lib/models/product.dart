import 'package:flutter/cupertino.dart';

class Product {
  int id, cateId, price, size;
  String name, image, description;
  Color color;
  Product({this.id, this.cateId, this.size ,this.name, this.image, this.color ,this.price, this.description});
}